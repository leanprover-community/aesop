#!/bin/bash

failed=0

leanpkg build
[ "$?" -eq 0 ] || exit 1

pushd tests &>/dev/null

for file in *.lean; do
  env LEAN_PATH="../build:." lean "$file"
  [ "$?" -eq 0 ] || failed=1
done

popd &>/dev/null

exit $failed
